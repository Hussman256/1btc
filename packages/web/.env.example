# URL of the 1btc index service (WebSocket). Defaults to ws://localhost:8787.
VITE_INDEX_URL=ws://localhost:8787

# URL of the 1btc clubs relay (NIP-29). Defaults to ws://localhost:8788.
VITE_CLUBS_RELAY=ws://localhost:8788
